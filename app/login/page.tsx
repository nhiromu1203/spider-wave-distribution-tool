import { redirect } from "next/navigation";

/**
 * ログイン画面は使っていない。
 *
 * ── 経緯 ────────────────────────────────────────────────────
 * 社内で使うツールで、利用者ごとの権限差が無いため、毎回ログインを
 * 求める意味が薄かった。現在は認証を挟まず業務画面を使える。
 *
 * 画面の実装（LoginForm / GoogleSignInButton / actions.ts）と
 * 判断ロジック（lib/auth）は消さずに残してある。ログインを戻す場合に
 * また書き起こす必要が無いようにするため。
 *
 * ここを開いた人が迷わないよう、業務画面へ送る。
 * ────────────────────────────────────────────────────────────
 */
export default function LoginPage() {
  redirect("/buildings");
}
